import styled from "@emotion/styled";
import { FC } from "react";

const Button = styled.button`
  padding: 32px;
`;

const StyledEmotionButton: FC = () => {
  return <Button>Button Components</Button>;
};

export default StyledEmotionButton;
